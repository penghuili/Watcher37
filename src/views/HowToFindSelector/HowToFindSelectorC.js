import { connect } from 'react-redux';

import HowToFindSelector from './HowToFindSelector';

const mapStateToProps = () => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(HowToFindSelector);
