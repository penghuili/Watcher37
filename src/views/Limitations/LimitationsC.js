import { connect } from 'react-redux';

import Limitations from './Limitations';

const mapStateToProps = () => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(Limitations);
